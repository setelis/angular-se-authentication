describe("SeAuthenticationService", function () {
	"use strict";

	beforeEach(module("seAuthentication.service", function() {
	}));
	beforeEach(inject(function () {
	}));

	it("no-tests-yet", inject(function () {
	}));
});
